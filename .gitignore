node_modules
dist
.env*
# data
# git add .
# git commit -m "Add new feature X"
# git push

# לסיים את הדך קונטקט חיבור למייל + בטיחות - משימת המשך להחליט אם יהיה בקאנד או לא קודם
# // הצהרת נגישות וכולי
# // Check if all works good in any res and any way and add last touches
# // build backend ? (edit,add and delete photos + drag and drop and real Auth)
# // Deploy and check

